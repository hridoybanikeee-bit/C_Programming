#include<stdio.h>
#include<string.h>
int main()
{
    char s[51], t[51];
    scanf("%s %s",s, t);
    // printf("%s\n%s",s,t);
    int a = strlen(s);
    int b = strlen(t);
    // for(int i=0; i<a; i++)
    // {
    //     printf("%c%c",s[i],t[i]);
    // }
    return 0;
}