#include<stdio.h>
int main()
{
    float a = 17;
    float b = 5;
    // int sum = a + b; //adding two digits
    // int sub = a - b; // subtracting two digits
    // int mul = a * b; //multiplying two digits
    float div = a / b; // Dividing two digits
    // int mods = a % b; // Reminder of two digits
    // printf("Sum: %d\n",sum);
    // printf("Subtraction: %d\n",sub);
    // printf("Multiplication: %d\n",mul);
    printf("Division: %.2f\n", div);
    // printf("Reminder: %d", mods);
    return 0;
}